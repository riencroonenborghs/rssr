.p-2
  = form_with(model: @subscription, url: [:admin, @subscription], method: :post, local: true) do |form|
    = form.hidden_field :step, value: @step
    .flex.flex-col
      %div(class="w-1/2")
        = form.label :url, "Website", class: "text-sm font-medium"
        .mt-2(class="ms-2 rounded-md ring-1 ring-gray-300 focus-within:ring-1 focus-within:ring-slate-600")
          = form.text_field :url, autofocus: true, class: "block border-0 bg-transparent py-1.5 pl-2 text-gray-900  focus:ring-0 sm:text-sm sm:leading-6 w-full", placeholder: "https://news.ycombinator.com/"
      - if @service && @service.errors.any?
        .text-red-500.text-xxs.pl-2
          = @service.errors.full_messages.to_sentence
      .mt-4
        = form.submit "Next", class: "rounded-md bg-slate-600 px-3 py-1.5 text-sm font-semibold leading-6 text-white shadow-sm hover:bg-slate-500 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-slate-600"
