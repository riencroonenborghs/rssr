- content_for :meta_title do
  Add your own RSS subscription

#new-sub
  .p-4.w-100
    %h4
      = link_to "Subscriptions", admin_subscriptions_path
      &gt; New

    = render "/admin/subscriptions/steps"

    = render "/admin/subscriptions/#{@step}"
