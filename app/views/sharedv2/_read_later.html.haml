.read-later-wrapper.is-flex.is-flex-direction-column.is-justify-content-center(class="read-later-#{entry.id}")
  %progress.progress.is-small.is-primary.is-hidden(max="100" style="width: 15px; margin: 0px;") 15%

  %span.read-later-read-it(class="#{'is-hidden' unless user_signed_in? && entry.read_later?(current_user)}")
    = link_to read_later_read_it_path(entry), remote: true, method: :delete, class: "read-later-read-it-link", title: "I've read it" do
      = fa_solid "star", size: 1
  %span.read-later(class="#{'is-hidden' if user_signed_in? && entry.read_later?(current_user)}")
    = link_to read_later_path(entry), remote: true, method: :post, title: "Read it later", class: "read-later-link" do
      = fa_regular "star", size: 1