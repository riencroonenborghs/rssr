- content_for :title do
  Tagged with "#{@tag}"

#tagged
  - unless mobile?
    %h6.pb-2 Tagged with "#{@tag}"

  = render "/shared/entries", entries: @entries, bookmarks: @bookmarks, viewed: @viewed
  