%span.flex.flex-row.flex-wrap
  - @tags[:subscription].each do |tag|
    = link_to tagged_feeds_path(CGI.escape(tag.upcase)) do
      .rounded.bg-emerald-400.text-white.text-xtiny.ps-1.pe-1.me-1.mb-1
        = tag.upcase
  - @tags[:entry].each do |tag|
    = link_to entry_tagged_feeds_path(CGI.escape(tag.upcase)) do
      .rounded.bg-emerald-600.text-white.text-xtiny.ps-1.pe-1.me-1.mb-1
        = tag.upcase
