
%span.flex.flex-row.flex-wrap
  - @tags[:subscription].each do |tag|
    .me-1
      = link_to tagged_feeds_path(tag.upcase) do
        = tag.upcase
  - @tags[:entry].each do |tag|
    .me-1
      = link_to entry_tagged_feeds_path(tag.upcase) do
        = tag.upcase